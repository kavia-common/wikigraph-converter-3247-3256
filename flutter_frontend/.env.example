# Backend base URL for API requests
# On Android emulator, use 10.0.2.2 to reach host machine localhost.
# On iOS simulator, use http://localhost:3001 if your backend runs on host.
BACKEND_BASE_URL=http://10.0.2.2:3001
